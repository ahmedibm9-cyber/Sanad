# Deployment

`npm ci`, typecheck, tests, and build were executed locally. No CI configuration or staging deployment configuration was found. Staging deployment, rollback, HTTPS/security headers, and controlled observability failure tests are BLOCKED_EXTERNAL. CI release gate is FAIL (CERT-004).
