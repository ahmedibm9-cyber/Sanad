# API Contract Review

## Protocol and Version
RESTful API (planned for Supabase integration)
Version: 1.0.0 (planned)

## Consumers Identified
1. SANAD Frontend Application (React/Vite)
2. Potential future mobile applications
3. Potential third-party logistics integrations
4. Administrative dashboards

## Compatibility Policy
Semantic Versioning with backward compatibility within major versions
Planned: Additive changes only for minor/patch versions
Breaking changes require major version increment and migration path

## Operations/Events Identified (from code analysis)
Based on the services layer and planned Supabase integration:

### Document Management
- GET /documents - List documents with filtering
- GET /documents/{id} - Get specific document
- POST /documents - Create new document
- PUT /documents/{id} - Update document
- DELETE /documents/{id} - Delete document (to trash)
- GET /documents/{id}/preview - Get document preview

### Customer Management
- GET /customers - List customers
- GET /customers/{id} - Get specific customer
- POST /customers - Create customer
- PUT /customers/{id} - Update customer
- DELETE /customers/{id} - Delete customer

### Project/Work Item Management
- GET /projects - List projects
- GET /projects/{id} - Get specific project
- POST /projects - Create project
- PUT /projects/{id} - Update project
- DELETE /projects/{id} - Delete project

### Material Management
- GET /materials - List materials
- GET /materials/{id} - Get specific material
- POST /materials - Create material
- PUT /materials/{id} - Update material
- DELETE /materials/{id} - Delete material

### Authentication & Authorization
- POST /auth/login - User login
- POST /auth/logout - User logout
- GET /auth/me - Get current user
- POST /auth/refresh - Refresh token

### Company & Settings
- GET /company - Get current company data
- PUT /company - Update company settings
- GET /settings - Get application settings
- PUT /settings - Update application settings

### Reporting & Analytics
- GET /reports - Get reports data
- GET /analytics - Get analytics data

## Semantic Invariants
1. All endpoints require authentication except public health checks
2. Company ID is required for all data operations (multi-tenancy)
3. Document numbers must be unique per company
4. Soft deletes only in UI (actual deletion requires admin/trash recovery)
5. All monetary values use decimal precision
6. Dates follow ISO 8601 format
7. Language preference affects response language for localizable fields
8. Pagination uses limit/offset or cursor-based approaches
9. Error responses follow consistent structure with message and code
10. Successful responses wrap data in standardized envelope

## Compatibility Findings
- Current implementation uses mock data layer
- Planned migration to Supabase will maintain same API contracts
- Backward compatibility planned for existing frontend
- Versioning strategy needed for future breaking changes

## Security and Privacy
- All endpoints require valid JWT tokens
- Row Level Security (RLS) planned for Supabase to enforce company isolation
- Sensitive data (financial info, personal data) protected via encryption
- Audit logging planned for sensitive operations
- CORS policies to restrict origins
- Rate limiting to prevent abuse

## Contract Tests
Recommended test suites:
1. Contract tests for each endpoint verifying:
   - Status codes for success/error cases
   - Response schema validation
   - Authentication requirements
   - Company data isolation
   - Input validation and sanitization
   - Edge case handling

2. Consumer-driven contract tests:
   - Frontend contract tests verifying expected API behavior
   - Mock server tests for frontend development

3. Integration tests:
   - Full flow tests for critical user journeys
   - Database migration tests
   - Backup and recovery tests

## Deprecations
None current (initial version)

## Migration and Sunset
Planned migration path:
1. Version 1.0.0: Initial Supabase backend with current contract
2. Version 1.1.0: Additive features only
3. Version 2.0.0: Any breaking changes with 1.0.0 deprecation notice

## Status
changes_required - API contract needs to be formalized and tested

## Next Steps
1. Create formal OpenAPI/Swagger specification
2. Implement contract testing in CI/CD pipeline
3. Define versioning and deprecation policy
4. Create consumer documentation
5. Implement monitoring for contract compliance

Review generated by ai-api-contract-guardian skill